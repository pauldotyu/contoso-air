apiVersion: v1
kind: Service
metadata:
  name: contoso-air
  namespace: demo
  labels:
    app.kubernetes.io/name: contoso-air
    kubernetes.azure.com/generator: devhub
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: contoso-air
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80